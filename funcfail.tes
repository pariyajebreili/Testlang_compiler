def int find(vector A, int n) {
    # this is a comment
    var int k;
    for(i = 0 to length(A)) {
        if (n == k) {
            return k;
            }
        j= j + 1;
    } 
    return -1;
}
def int main() {
    var int A;
    var int a;
    A = list(3);
    A[0] = 1;
    A[1] = 2;
    A[2] = 3;
    print(find(A, a));
    print(find(A));
    print(find(a, A));
}

  
